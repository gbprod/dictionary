<?xml version="1.0" ?>
<project name="Meup Dictionary" basedir="./" default="help">

    <target name="help">
        <echo>
        help     Shows this help
        tests    Run tests
        phpcs    Detects standard violations on the code.
        phpcpd   Detects duplicate code portions.
        phpunit  Executes phpunit tests
        </echo>
    </target>

    <target name="test" depends="phpcs,phpcpd,phpunit">
    </target>

    <target name="phpcs">
        <phpcodesniffer standard="PSR1">
            <fileset dir="./src">
                <include name="**/*.php"/>
            </fileset>
        </phpcodesniffer>
        <phpcodesniffer standard="PSR2">
            <fileset dir="./src">
                <include name="**/*.php"/>
            </fileset>
        </phpcodesniffer>
    </target>

    <target name="phpcpd">
        <exec logoutput="true" dir="./" command="./vendor/bin/phpcpd ./src" />
    </target>

    <target name="phpunit">
        <exec logoutput="true" dir="./" command="./vendor/bin/phpunit" />
    </target>

</project>